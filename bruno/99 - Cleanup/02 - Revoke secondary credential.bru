meta {
  name: Revoke secondary credential
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/admin/clients/{{testClientId}}/credentials/{{rotatedCredentialId}}
  body: none
  auth: none
}

headers {
  X-Client-Id: admin
  X-Internal-Api-Key: {{adminApiKey}}
}

tests {
  test("HTTP 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
