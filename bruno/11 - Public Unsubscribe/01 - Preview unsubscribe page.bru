meta {
  name: Preview unsubscribe page
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/public/unsubscribe?token=invalid-token
  body: none
  auth: none
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
