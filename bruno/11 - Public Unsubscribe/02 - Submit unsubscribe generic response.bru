meta {
  name: Submit unsubscribe generic response
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/public/unsubscribe
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  token: invalid-token
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
