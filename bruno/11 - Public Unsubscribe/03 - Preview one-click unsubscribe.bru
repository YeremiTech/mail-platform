meta {
  name: Preview one-click unsubscribe
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/public/unsubscribe/one-click?token=invalid-token
  body: none
  auth: none
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
