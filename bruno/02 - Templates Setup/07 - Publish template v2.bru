meta {
  name: Publish template v2
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/templates/{{templateSlug}}/versions/2/publish
  body: none
  auth: none
}

headers {
  X-Client-Id: {{testClientId}}
  X-Internal-Api-Key: {{clientApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
