meta {
  name: Publish template v1
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/templates/{{templateSlug}}/versions/1/publish
  body: none
  auth: none
}

headers {
  X-Client-Id: {{testClientId}}
  X-Internal-Api-Key: {{clientApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
