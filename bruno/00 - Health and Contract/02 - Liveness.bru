meta {
  name: Liveness
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/actuator/health/liveness
  body: none
  auth: none
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
