meta {
  name: Readiness
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/actuator/health/readiness
  body: none
  auth: none
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
