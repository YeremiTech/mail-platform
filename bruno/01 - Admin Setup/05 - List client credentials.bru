meta {
  name: List client credentials
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/admin/clients/{{testClientId}}/credentials
  body: none
  auth: none
}

headers {
  X-Client-Id: admin
  X-Internal-Api-Key: {{adminApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
