meta {
  name: List dead outbox events
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/admin/outbox/dead?limit=50
  body: none
  auth: none
}

headers {
  X-Client-Id: admin
  X-Internal-Api-Key: {{adminApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
