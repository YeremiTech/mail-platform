meta {
  name: Get delivery attempts
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/emails/{{mailId}}/attempts
  body: none
  auth: none
}

headers {
  X-Client-Id: {{testClientId}}
  X-Internal-Api-Key: {{clientApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
