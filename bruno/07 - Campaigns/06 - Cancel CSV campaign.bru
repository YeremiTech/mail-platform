meta {
  name: Cancel CSV campaign
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/campaigns/{{csvCampaignId}}
  body: none
  auth: none
}

headers {
  X-Client-Id: {{testClientId}}
  X-Internal-Api-Key: {{clientApiKey}}
}

tests {
  test("HTTP 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
