meta {
  name: Remove suppression
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/suppressions?email={{suppressionEmail}}
  body: none
  auth: none
}

headers {
  X-Client-Id: {{testClientId}}
  X-Internal-Api-Key: {{clientApiKey}}
}

tests {
  test("HTTP 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 30000
}
